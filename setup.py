from setuptools import setup, find_packages

setup(
    name='authservice_grpc',
    version='0.0.1',
    packages=find_packages(),
    install_requires=[
        # Здесь перечислите зависимости вашего пакета
    ],
)